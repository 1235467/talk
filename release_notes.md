# Talk v0.1.45

本次更新集中修复存档隔离和几处常用交互，并完善管理员 AI 测试卡的运行隔离。

## 主要更新

- 修复消息首页长按菜单错位，以及长按后误进入聊天的问题。
- “以前创建过的人设”现在可以由用户主动删除，但仍不会被系统自动清理。
- 仓库恢复为一件物品一张卡，支持丢弃；复购入口移到商城，并保留独立购买历史。
- 修复结构化 AI 记忆跨存档残留；回档会停止旧存档后台任务并刷新运行状态。
- 用户和 AI 发布的朋友圈都可以撤销，点赞、评论和社交事件会一并清理。
- 联系人资料页新增“TA 的朋友圈”入口。
- AI 测试卡数据与正常联系人、会话、通知和后台模拟隔离。

## 数据兼容

- 数据库升级到 v32，旧的堆叠库存会自动拆成独立物品，同时生成商城复购历史。
- 备份格式升级到 v4；旧备份仍可导入，旧备份中缺失的结构化记忆会按空数据处理，不再沿用当前存档记忆。

## 验证

- TypeScript 生产构建、Lint 和 112 项单元测试通过。
- Chromium Mobile 端到端测试 38 项通过，1 项按既有条件跳过。
- APK 与 Windows 安装包均使用空 API Key 构建，并通过敏感值扫描。

## 下载

- `Talk-0.1.45-android-debug.apk`：Android 调试签名安装包。
- `Talk-0.1.45-x64.exe`：Windows x64 安装版。
- `Talk-0.1.45-portable-x64.exe`：Windows x64 便携版。
