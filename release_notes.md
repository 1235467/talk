# Talk v0.1.40 — 全端图标统一与桌面聊天输入区重构

本次更新集中解决两类长期影响整体质感的问题：普通界面混用 Emoji 导致视觉语言不统一，以及 Windows 桌面端聊天输入区缺少边界和桌面应用应有的空间感。

## 重点更新：全端统一 Lucide 图标

- Android、移动浏览器与 Windows 桌面端的导航、功能入口、操作按钮、标题装饰和状态提示统一改用 `Lucide React`。
- 新增统一的 `UiIcon` 图标入口，规定 18–20px 常用尺寸、1.8 线宽与 `currentColor` 继承，图标会自然跟随 Sage、Forge、Fox、Ink、Nord、WeTalk 六套主题及浅色/深色模式。
- 替换发现页、模块管理、设置、联系人创建、联系人名片、朋友圈、聊天气泡、互动收件箱、表情包管理、桌面侧栏和窗口控制按钮等区域原先承担 UI 功能的 Emoji 或文本符号。
- 明确 Emoji 的保留边界：头像、心情、商品/礼物、地图地标、自定义货币等用户内容或业务数据仍保留原本表现，不会被机械替换。

## 重点更新：Windows 桌面聊天输入区

- PC 端聊天输入区改为更接近微信桌面版的大输入面板，增加明确的外边距、边框和底部留白，聊天区域不再显得贴边或缺少边界。
- 文本输入位于面板上部，常用功能按钮内嵌在左下角，发送按钮固定在右下角。
- 桌面端移除原来的“加号展开”入口，表情搜索、AI 代写、转账、红包、借款、还款、工作、商城和仓库等功能可以直接从工具栏进入。
- 工具图标会跟随模块开关：关闭职业、商城或仓库模块后，对应桌面工具和手机功能项会同步隐藏。
- 保留 Enter 发送、Shift + Enter 换行；较矮的桌面窗口会自动缩减输入面板高度。
- 手机端继续使用适合触屏的紧凑输入栏和加号功能面板，不会照搬桌面布局。

## 规范与后续一致性

- 更新 `docs/DESIGN_SYSTEM.md` 与项目 `AGENTS.md`，将 Lucide 使用方式、Emoji 允许范围以及桌面输入区布局写成强制规范。
- 后续新增普通 UI 图标时必须复用 Lucide；不得重新用 Emoji 充当导航、按钮、标题装饰或状态图标。
- 普通组件继续消费统一主题令牌，图标不维护独立色板，确保 Android 与 Windows、浅色与深色表现一致。

## 验证

- 完成 WeTalk 深色桌面端与 Sage 浅色手机端视觉检查。
- 390px 手机宽度和 1440px 桌面布局均无横向溢出。
- 主题源码与正式构建产物检查通过。
- TypeScript 正式构建与 lint 通过。
- 单元测试 100 项通过，3 项按既有配置跳过。

## 下载说明

- `Talk-0.1.40-android-debug.apk`：Android 安装包，使用 debug 签名，可直接侧载安装。
- `Talk-0.1.40-x64.exe`：Windows x64 安装版。
- `Talk-0.1.40-portable-x64.exe`：Windows x64 便携版。
- 发布产物不内置本地 DeepSeek、Tavily 或 Pexels API Key；首次使用请在应用设置中自行填写。
